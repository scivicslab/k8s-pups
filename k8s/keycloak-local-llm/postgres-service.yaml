apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres
  namespace: keycloak-local-llm
spec:
  selector:
    app: keycloak-postgres
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
