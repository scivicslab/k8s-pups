# EnvoyProxy: pin NodePort 31088 for Apache routing on stonefly501
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: k8s-pups-local-llm-proxy
  namespace: k8s-pups-local-llm
spec:
  logging:
    level:
      default: warn
  provider:
    type: Kubernetes
    kubernetes:
      envoyService:
        externalTrafficPolicy: Cluster
        patch:
          type: StrategicMerge
          value:
            spec:
              ports:
              - name: http-80
                nodePort: 31088
                port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: k8s-pups-local-llm-gateway
  namespace: k8s-pups-local-llm
spec:
  gatewayClassName: envoy
  infrastructure:
    parametersRef:
      group: gateway.envoyproxy.io
      kind: EnvoyProxy
      name: k8s-pups-local-llm-proxy
  listeners:
  - name: http
    port: 80
    protocol: HTTP
    allowedRoutes:
      namespaces:
        from: Same
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: k8s-pups-local-llm-route
  namespace: k8s-pups-local-llm
spec:
  parentRefs:
  - name: k8s-pups-local-llm-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /local-llm
    filters:
    - type: RequestHeaderModifier
      requestHeaderModifier:
        set:
        - name: X-Forwarded-Proto
          value: https
    backendRefs:
    - name: k8s-pups-controller
      port: 8080
