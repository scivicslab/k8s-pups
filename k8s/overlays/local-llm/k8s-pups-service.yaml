apiVersion: v1
kind: Service
metadata:
  name: k8s-pups-controller
  namespace: k8s-pups-local-llm
spec:
  selector:
    app: k8s-pups-controller
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
